#include <stdio.h>

int main()
{
  double x;
  int cont, quant;
  
  quant=0;
  for(cont=1; cont<=6; cont++){
    scanf("%lf", &x);
    if(x>0){
      quant++;
    }
  }
  printf("%d valores positivos\n", quant);
  return 0;
}
